on:
  workflow_dispatch:
  push:
    branches: main

name: Quarto Publish

# Required permissions for GitHub Pages direct deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Only allow one concurrent deployment to prevent conflicts
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      # Renders figure SVGs with Deno, then renders Quarto (both fetched via uv)
      - name: Build and Render
        run: make

      # Packages the _acuity output directory and uploads it to GitHub infrastructure
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _acuity

      # Deploys the artifact to your live GitHub Pages URL
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
